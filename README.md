# Materia Medica Quiz tool

Run start.bat script.
start.bat and MasteringOfMateriaMedica-1.0-jar-with-dependencies.jar
must be in the same directory as directory remedies which should contain remedy symptoms.
In such manner as in example. 
  